<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Agregar idioma</title>
</head>
<body>
    <h1>Agregar idioma</h1>
    <a href="/empleado_idiomas/empleado/<%= codigo_empleado %>/idiomas">regresar</a>
    <form action="/empleado_idiomas/empleado/<%= codigo_empleado %>/crear" method="post">

        <label for="idiomas">Seleccione un idioma:</label><br>
        <select id="idiomas" name="codigo_idioma" required>
            <option value="" disabled selected>-- Seleccione un idioma --</option>
            <% idiomas.forEach(idioma => { %>
                <option value="<%= idioma.codigo %>"><%= idioma.idioma %></option>
            <% }); %>
        </select><br>

        <label for="fecha">Tiempo de saber idioma(fecha):</label><br>
        <input type="text" name="fecha" placeholder="2000-12-31"><br>

        <button type="submit">Confirmar</button>
    </form>
</body>
</html>
