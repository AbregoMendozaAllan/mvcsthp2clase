<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Eliminar idioma</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

    <style>
        .container {
            max-width: 900px;
            margin: auto;
            padding-top: 20px;
        }

        a {
            margin-right: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        td, th {
            padding: 10px;
            text-align: left;
        }

        .buttons {
            margin-top: 20px;
        }

        .form-container {
            margin-top: 30px;
        }

        .form-container input {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        button {
            background-color: #ff3860;
            color: white;
            border-radius: 5px;
            padding: 10px 15px;
            cursor: pointer;
        }

        button:hover {
            background-color: #e02948;
        }
    </style>
</head>
<body>

<div class="container">
    <h1 class="title is-3">Eliminar idioma</h1>

    <a class="button is-info" href="/empleado_idiomas/empleado/<%= empleado_idioma.codigo_empleado %>/idiomas">Regresar</a>

    <div class="form-container">
        <form action="/empleado_idiomas/empleado/<%= empleado_idioma.codigo_empleado %>/eliminarIdioma/<%= empleado_idioma.codigo_idioma %>" method="post">
            <div class="field">
                <label for="idioma" class="label">Idioma:</label>
                <div class="control">
                    <input type="text" name="idioma" class="input" value="<%= empleado_idioma.idioma %>" disabled>
                </div>
            </div>

            <div class="field">
                <label for="fecha" class="label">Tiempo de saber idioma (fecha):</label>
                <div class="control">
                    <input type="text" name="fecha" class="input" value="<%= empleado_idioma.fecha %>" disabled>
                </div>
            </div>

            <div class="field">
                <div class="control">
                    <button type="submit" class="button is-danger">Confirmar</button>
                </div>
            </div>
        </form>
    </div>
</div>

</body>
</html>
