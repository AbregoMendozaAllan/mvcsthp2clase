<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Idiomas</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

    <style>
        .container {
            max-width: 900px;
            margin: auto;
            padding-top: 20px;
        }

        a {
            margin-right: 10px;
        }

        a img {
            vertical-align: middle;
            transition: transform 0.2s;
        }

        a:hover img {
            transform: scale(1.1);
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead {
            background-color: #000100;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f5f5f5;
        }

        td, th {
            padding: 10px;
            text-align: left;
        }

        .buttons {
            margin-top: 20px;
        }

    </style>
</head>
<body>

<div class="container">
    <h1 class="title is-3">Lista de Idiomas</h1>

    <div class="buttons">
        <a class="button is-info" href="/index">Regresar a Indice</a>
        <a class="button is-primary" href="/idiomas/crear">Agregar idioma</a>
    </div>

    <table class="table is-striped is-hoverable is-fullwidth">
        <thead>
        <tr>
            <th>Código</th>
            <th>Idioma</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        <% idiomas.forEach(idioma => { %>
            <tr>
                <td><%= idioma.codigo %></td>
                <td><%= idioma.idioma %></td>
                <td>
                    <a href="/idiomas/editar/<%= idioma.codigo %>">
                        <img src="static/resources/edit_square_24dp.svg" alt="Editar" width="24" height="24">
                    </a>
                    <a href="/idiomas/ver/<%= idioma.codigo %>">
                        <img src="static/resources/visibility_24dp.svg" alt="Ver" width="24" height="24">
                    </a>
                    <a href="/idiomas/eliminar/<%= idioma.codigo %>">
                        <img src="static/resources/delete_24dp.svg" alt="Eliminar" width="24" height="24">
                    </a>
                </td>
            </tr>
        <% }) %>
        </tbody>
    </table>
</div>

</body>
</html>
